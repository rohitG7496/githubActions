name: first demo workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-24.04
    steps:
      - name: first demo
        run: echo "first demo workflow"
      - name: print done
        run: echo "done!" 
